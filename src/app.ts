import type { Application } from 'express';

// TODO: assemble the Express app (middleware, routes, error handling). Do not call listen() here.
export function createApp(): Application {
  throw new Error('Not implemented');
}
